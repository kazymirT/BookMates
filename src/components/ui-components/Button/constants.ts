export enum Sizes {
  Small = 'small',
  Big = 'big',
  Full = 'full-width',
}

export enum Variant {
  Basic = 'basic',
  Primary = 'primary',
}

export enum ButtonType {
  Submit = 'submit',
  Button = 'button',
}

export enum Position {
  Left = 'left',
  Right = 'right',
}
