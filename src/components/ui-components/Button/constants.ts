export enum Sizes {
  Small = 's',
  Medium = 'm',
  Large = 'l',
  ExtraLarge = 'extra_l',
  Full = 'full',
}

export enum Variant {
  Basic = 'basic',
  Primary = 'primary',
}

export enum ButtonType {
  Submit = 'submit',
  Button = 'button',
}

export enum Position {
  Left = 'left',
  Right = 'right',
}
